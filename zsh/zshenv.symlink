# Keep zsh configuration under ~/.config/zsh, matching the xonsh layout.
export ZDOTDIR="$HOME/.config/zsh"

# Codex runs non-login, non-interactive zsh, so .zshrc is skipped.
if [[ -n "${CODEX_SHELL:-}" ]]; then
  source "$ZDOTDIR/.zshrc"
fi
